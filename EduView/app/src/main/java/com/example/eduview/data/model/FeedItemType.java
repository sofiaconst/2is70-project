package com.example.eduview.data.model;

/**
 * Enum representing the types of feed items in the feed.
 */
public enum FeedItemType {
    //Important message from teachers
    ANNOUNCEMENT,
    //Post awaiting approval before becoming visible
    PENDING,
    // Standard post visible to all users
    PUBLISHED
}
