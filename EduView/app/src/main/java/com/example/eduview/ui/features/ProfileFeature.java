package com.example.eduview.ui.features;

public interface ProfileFeature {
    /** Show the feature (bind data, make views visible) */
    void show();

    /** Hide the feature (remove visibility, cleanup if needed) */
    void hide();
}
